# API Documentation

The REST API is versioned under `/api/v1`. Interactive documentation is served
by FastAPI at `/docs` (Swagger UI) and `/redoc` when the backend is running.

Phase 1 exposes no business endpoints — only the `/health` liveness probe.
Endpoint reference material is added here as each module is implemented.
