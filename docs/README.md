# LRS Documentation

| Directory | Contents |
|---|---|
| [`architecture/`](architecture/) | Role hierarchy, department isolation, layering rules, [authentication design](architecture/authentication.md) (Phase 3A), [authorization/RBAC design](architecture/authorization.md) (Phase 3B.1), [department management](architecture/department-management.md) (Phase 3B.2), [Admin management](architecture/admin-management.md) (Phase 3B.3), [User management](architecture/user-management.md) (Phase 3B.4), the [Letter Registry Core](architecture/letter-registry.md) (Phase 4A review + Phase 4B implementation), [Registry Operations & Search](architecture/registry-search.md) (Phase 4C — pagination, sorting, and search on `GET /api/v1/letters`), [Document Management](architecture/document-management.md) (Phase 4D — `LetterDocument` upload/list/download implemented on top of this phase's own architecture review; no deletion endpoint, a deliberate scope decision), [Operational Activity, Notifications & Audit](architecture/audit-notifications.md) (Phase 4E — `AuditLog` generation for Letter/Document/User/Admin/Department/Category/Classification/Authorization events, `Notification` generation for the confirmed "letter registered" trigger, implemented on top of this phase's own architecture review; no audit read API, a deliberate scope decision), [Frontend & Operational UI](architecture/frontend.md) (Phase 5 — architecture/UX review of the React 18 + Vite skeleton against all 42 backend endpoints; Phase 5A then implemented the foundation the review designed — routing, auth state, API client, protected routes, role navigation, app shell; Phase 5B then implemented the complete login/signup/pending-approval/deactivated-account/session-restoration/logout experience; Phase 5C then implemented the complete Letter registry — list/search/sort/paginate/create/view/edit/archive — including resolving a confirmed category/classification reference-data access gap rather than working around it; Document/Notification UI built later, in Phase 5E), [Administration & Account Management UI](architecture/administration-ui.md) (Phase 5D — architecture/requirements review, then implementation, of the Department/Admin/User management frontend against the actual Department/Admin/User backend contract; confirmed no pagination/search/sort exists on any of those four resources unlike Letters, corrected the phase brief's own incorrect assumption about `AuthorizationStatus`'s values, and built the full System-Admin (Departments, Administrators, transfer) and Admin (Users, authorization workflows) administration UX against confirmed backend behavior only — Category/Classification management UI remains unbuilt, out of this phase's own scope), and [Documents & Notifications UI](architecture/document-notification-ui.md) (Phase 5E — architecture/requirements review, then implementation, of the Document upload/list/download and Notification frontend against the actual `documents.py`/`notifications.py` backend contract; confirmed fetch+blob is required for downloads (no plain `<a href>` can carry the bearer token), no document delete/replace endpoint exists, notification recipient isolation and message content are both structurally safe to render as-is, and built `DocumentList`/`DocumentUploadForm` into `LetterDetailPage`, `NotificationBell`/`NotificationPanel`/`NotificationItem` into `Topbar` and a real paginated `/app/notifications` page, with explicit-button-only mark-read — an explicit override of the review's own provisional lean — and no frontend authorization rule added anywhere; Category/Classification management UI remains unbuilt, out of this phase's own scope), and [Dashboard & Operational Overview UI](architecture/dashboard.md) (Phase 5F — architecture/requirements review, then implementation, of a Dashboard frontend against all twelve mounted business routers; confirmed no dashboard/summary/aggregate/audit-read endpoint exists anywhere, that `GET /letters`'s `total` is a real, already department/classified-visibility-scoped SQL `COUNT` while `GET /departments`/`/admins`/`/users`/`/categories`/`/classifications` have no pagination and compute `total` as `len()` in Python, and built `/app/dashboard` — role-aware Letter/Notification summary cards for every role, Department/Admin or User/approval cards for SYSTEM_ADMIN/ADMIN, a Recent Letters list, and role-scoped Quick Actions to already-existing screens only — entirely from existing, already-isolated endpoints, with no new backend endpoint, no chart, and no analytics infrastructure of any kind), and [Backend Dashboard Aggregation & Analytics API](architecture/dashboard-analytics-api.md) (Phase 5G — architecture/requirements review only, no code written, of whether the backend should provide additional aggregate/analytics endpoints; confirmed the existing `letter_visibility_filter`/department-derivation authorization logic is directly reusable for any future Letter aggregate, that every plausible aggregate dimension is already indexed (zero new indexes recommended), and that `AuditLog` has no department column at all (audit analytics deferred to its own future phase); a full metric inventory found no metric with both confirmed value and no existing sufficient API, recommending backend aggregation be deferred entirely for V1 with a complete, ready-to-build `GET /api/v1/letters/aggregate` design documented but not implemented), and [Source Department & Designation Master Data](architecture/source-designation.md) (Phase 5H — architecture/requirements review, then implementation, of two handover-demo requirements; confirmed `source_department_id` already existed end-to-end on every Letter schema with existing backend validation (a mostly-frontend task), and that `GET /api/v1/departments` being `require_system_admin`-only would have silently blocked Source Department and a new Designation dropdown for USER/ADMIN — fixed by relaxing both list endpoints to any authenticated role, every write endpoint unaffected; built a new `Designation` master-data resource (model/repository/service/API/a minimal SYSTEM_ADMIN management page) mirroring `Category`/`Classification`, with historical integrity solved via a new nullable `designation_id` FK (migration `323ccfde77f4`, verified upgrade/downgrade/upgrade/check against a real database) alongside the existing, unchanged `sender_designation` text — zero backfill; `LetterFormPage.jsx` gained Source Department and Designation selectors, both required only on create; test suite grown to 487 backend / 280 frontend tests), Phase 5H.1 (Complete Existing Category & Classification Admin UI — a confirmed frontend completion gap found during Phase 5H's own manual E2E pass: `/app/system/categories`/`/app/system/classifications` still rendered a "planned" placeholder despite the backend, unchanged since Phase 4B, already supporting list/create/update/activate/deactivate; six new pages closed the gap, mirroring the existing Department three-page pattern, with zero backend files touched and no delete action added; test suite grown to 320 frontend tests), and [Futuristic UI / Visual Architecture & Design System](architecture/ui-design-system.md) (Phase 5I — a review-only inspection of the actual current frontend's colors/typography/spacing/components/tests against a "futuristic enterprise command center" design direction; found a real color-token bug and several duplicated CSS patterns, and proposes an extended token system, navigation/loading/status-system redesigns, and an AJ-OVA Labs footer, all achievable with zero new dependencies — no code, test, dependency, backend, or database file was changed; Phase 5I.1 then implemented the global foundation only — extended `tokens.css`/`global.css` and fixed the confirmed color bug, with no page or component redesigned; Phase 5I.2 then implemented the App Shell & Navigation layer — Sidebar/Topbar redesign, a mobile drawer, and the AJ-OVA Labs footer wired into `AppShell`, with `navigationConfig.js`'s role-derived list unchanged; Phase 5I.3 then implemented the Core UI Primitives layer — a new shared `styles/primitives.module.css` for buttons/tables/dialogs, a global form-control base, a StatusBadge shape layer, and an EmptyState glyph, with Dashboard/Letter-page/Administration-page/Document-page/Notification-panel/Authentication-page redesign explicitly deferred; Phase 5I.4A then delivered the first screen-level visual transformation — `/app/dashboard` recomposed into a header/metrics/activity/actions layout, with every metric, fetch, and role branch confirmed unchanged; Phase 5I.4B then delivered the second — the Letter registry family (`LetterListPage`/`LetterFormPage`/`LetterDetailPage`/`LetterFilters`/`LetterTable`) recomposed into a registry header, a "Registry Search" filter console, an accent-barred table, and a four-section record dossier, with every field, filter, URL parameter, and role branch confirmed unchanged; Phase 5I.4C then delivered the third — the entire administration workspace (Departments/Administrators/Users/Authorizations/Designations/Categories/Classifications), achieved mainly by enhancing the two files nearly every one of those ~28 pages already shared, plus a genuinely restructured `AdminTransferDialog` with its required wording confirmed byte-for-byte unchanged; Phase 5I.4D then delivered the fourth — Documents (within the Letter dossier, gaining a real non-fabricated attachment count and a percentage-bound upload progress bar) and Notifications (an "Operational Signals" eyebrow, a CSS-only bell connector, a static unread dot, and the same chip-count header language on the full notification page), with `NotificationBell` audited and left unmodified; Phase 5I.4E then delivered the fifth — the unauthenticated Login/Signup entrance, confirmed to have been the generic "white card + email + password + blue button" pattern, now sharing one static brand identity (the Sidebar's nested-square mark, `APP_NAME`, and the previously-unrendered `PRODUCTION_CREDIT` line) distinguished only by a small eyebrow label, with the submit button finally composed onto the shared button primitives and both account-state notices gaining a small color marker; Phase 5I.5 then delivered the application's boot/loading identity — a new `BootScreen` gated at `App.jsx` using `AuthContext`'s own existing `status === 'loading'` window (never a duplicated timer), presenting a CSS-only "LRS Registry Glyph" (the same nested-square geometry, plus four ticks illuminating in sequence) and a truthful accessible status message, with `AuthContext.jsx` and every other existing loading state left untouched; and Phase 5I.6 then closed the visual arc with a final polish/handover audit across all nine prior phases — a codebase-wide search (colors, motion, breakpoints, emoji, branding, and the full security-pattern set) that found and fixed exactly one genuine defect (`NotificationBell`'s emoji icon, replaced with a CSS-only outline, zero behavior change) and documented several other reviewed-and-confirmed-correct candidates rather than "fixing" what wasn't broken, with manual browser verification honestly reported as not performed (no browser-automation tool available); and Phase 5I.6A then made a small, targeted correction found during that audit's own manual review — the Sidebar's navigation "icons" were actually 3-letter monograms, replaced with 9 small inline-SVG geometric icons that follow the same existing active-state color mechanism, with `navigationConfig.js`/routes/labels/permissions untouched — each documented in the same file's own implementation-record sections) — all complete, deployment topology (planned), decision records (planned) |
| `api/` | REST API reference for `/api/v1` — placeholder; endpoints are documented in `architecture/authentication.md` §11 (auth), `architecture/authorization.md` §7 (verification-only), `architecture/department-management.md` §7 (departments), `architecture/admin-management.md` §10-11 (admins), `architecture/user-management.md` §4/§8/§9/§10 (users), and `architecture/letter-registry.md` §7-9 (letters, categories, classifications) ahead of a dedicated API reference in a later phase |
| [`database/`](database/) | Schema design, entity relationships, migration guide (Phase 2 — complete) |
| `user-guides/` | End-user and administrator guides per role — placeholder until roles have a UI |

Documentation is written alongside each module as it is implemented.
[`PROJECT_STATUS.md`](PROJECT_STATUS.md) is the current, supervisor-facing
summary of what's done, in progress, and pending S&IT confirmation.

Technical documentation and UI/UX support: **Faiza**.
