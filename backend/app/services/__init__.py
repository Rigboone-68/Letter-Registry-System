"""Service / business-logic layer.

Services own workflow and policy. They call repositories for persistence and
are the only layer the API is allowed to depend on for business decisions.

PHASE 1: empty by design.
"""
