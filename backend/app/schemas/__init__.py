"""Pydantic schemas — the API's request/response contracts.

PHASE 1: empty by design. Schemas are kept separate from ORM models so the
wire format can evolve independently of the database shape.
"""
