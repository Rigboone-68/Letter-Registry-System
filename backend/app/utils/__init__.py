"""Framework-agnostic helpers shared across layers.

Utilities must not import from `api`, `services`, or `repositories` — this
keeps the dependency direction one-way and the modules loosely coupled.

PHASE 1: empty by design.
"""
