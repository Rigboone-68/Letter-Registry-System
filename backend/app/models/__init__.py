"""SQLAlchemy ORM models.

PHASE 1: empty by design. No models, tables, or migrations exist yet.
Future modules (users, departments, letters, categories, attachments,
notifications) will each get their own module in this package and must be
imported in `app/database/base.py` for Alembic to detect them.
"""
