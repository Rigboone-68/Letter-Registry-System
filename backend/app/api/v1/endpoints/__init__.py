"""Endpoint routers for API v1.

PHASE 1: empty by design. One module per resource in later phases
(auth, users, departments, letters, categories, documents, notifications),
each exposing an APIRouter that the v1 aggregate router includes.
"""
